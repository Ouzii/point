import React from 'react'

type ComparisonTaskProps = {
    coords: Array<any>
    nextStep: () => void
}
export default ({ coords, nextStep }: ComparisonTaskProps) => {
    return (
        <div>

        </div>
    )
}